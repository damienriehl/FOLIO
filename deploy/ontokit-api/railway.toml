[build]
dockerfilePath = "Dockerfile.prod"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
startCommand = "uvicorn ontokit.main:app --host 0.0.0.0 --port 8000 --workers 4"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[service]
internalPort = 8000
