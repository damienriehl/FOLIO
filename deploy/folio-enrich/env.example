# folio-enrich Railway environment variables
# Set these in the Railway dashboard under your service's Variables tab

# At least one LLM API key is required
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Optional: configure the host/port (Railway sets PORT automatically)
HOST=0.0.0.0
PORT=8000
